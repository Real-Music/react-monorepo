{
  "name": "@koma-analytics/ui",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npm run build",
        "cwd": "packages/ui"
      },
      "outputs": ["{projectRoot}/dist"]
    }
  }
}
